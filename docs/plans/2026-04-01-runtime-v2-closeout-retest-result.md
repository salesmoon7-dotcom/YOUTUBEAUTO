# Runtime V2 Closeout Retest Result - 2026-04-01

## Purpose

- Satisfy the active closeout retest deliverable for `one closeout retest result interpreted only by current evidence`.
- Record the current `row15` closeout reading without pretending that probe/process success equals semantic-row completion.
- Freeze the latest target-row judgment using only current evidence, without overstating deferred `Canva` hold scope beyond what this row actually exercised.

## Target

- semantic target row: `Sheet1!row15`
- CLI mapping: `--row-index 14`

## Required Closeout Contract

The active plan recognizes closeout only when a new target-row `probe_root` contains both:

1. `probe_result.json`
2. either `render_final.mp4` or `failure_summary.json`

Anything less is not a closed retest result.

## Current Evidence Reading

| Evidence item | What it proves | What it does not prove |
|---|---|---|
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row1-rerun-03\probe_result.json` + `render_final.mp4` | generic row1 stage5 path once produced a closed success artifact | does not prove `row15` closeout |
| `D:\YOUTUBEAUTO_RUNTIME\probe\semantic-row-closeout-20260318-02\probe_result.json` | one row15 semantic rerun failed with `CHATGPT_RESPONSE_TIMEOUT` | not a successful closeout |
| `D:\YOUTUBEAUTO_RUNTIME\probe\semantic-row-closeout-20260318-03\probe_result.json` | a later row15 semantic rerun failed with `ADAPTER_TIMEOUT` | not a successful closeout |
| `D:\YOUTUBEAUTO_RUNTIME\probe\semantic-row-closeout-20260411-02\probe_result.json` + `D:\YOUTUBEAUTO_RUNTIME\probe\semantic-row-closeout-20260411-02\evidence\failure_summary.json` | an older row15 detached closeout once satisfied the terminal failure artifact contract and closed as a failed retest with `missing_scene_prompts` | no longer represents the latest row15 closeout truth |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-b\probe_result.json` + `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-b\evidence\failure_summary.json` | one fresh sequential row15 rerun advanced through `chatgpt -> qwen3_tts -> genspark ref-1 -> seaart ref-2 -> genspark main -> seaart main` and then closed at `canva / CANVA_PRODUCT_BACKGROUND_NO_PROMPT_INPUT` | does not prove that the latest closeout truth stays pinned at Canva under all fresh reruns |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-e\probe_result.json` + `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-e\evidence\failure_summary.json` | a later truly sequential row15 rerun still closed contractually, but failed earlier at `genspark / BROWSER_UNHEALTHY` | does not disprove the later Canva boundary; it shows the closeout truth was still oscillating between pinned single boundaries |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-h\probe_result.json` + `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-h\evidence\failure_summary.json` | one fresh truly sequential row15 rerun advanced through `chatgpt -> qwen3_tts -> genspark ref-1 -> seaart ref-2 -> genspark main -> seaart main` and then closed at `canva / CANVA_PRODUCT_BACKGROUND_NO_PROMPT_INPUT` | no longer represents the latest row15 closeout truth |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-j\probe_result.json` + `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-j\evidence\failure_summary.json` | an earlier fresh truly sequential row15 rerun advanced through `chatgpt -> qwen3_tts -> genspark ref-1 -> seaart ref-2 -> genspark main -> seaart main` and then closed at `canva / CANVA_PRODUCT_BACKGROUND_CREDIT_EXHAUSTED` | no longer represents the latest closeout truth |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260520-h-port-poll.jsonl` | during that earlier rerun, Genspark port `9333` kept serving `/json/version` and tabs continuously, and its top tab moved from compose to result (`agents?id=...`) mid-run | does not prove Genspark can never regress again, but it does show that row15 could advance without the browser port disappearing |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row1-target-16-18` partial voice outputs only | some downstream files were emitted during a hidden rerun | does not count as closeout because `probe_result.json`, `qwen3_result.json`, `failure_summary.json`, and final render evidence were missing |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260524-a\probe_result.json` + `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-20260524-a\artifacts\chatgpt\chatgpt-sheet1-15\a0840661-17a0-44bf-afb7-b68657bd1ece\assets\output\render_final.mp4` | latest fresh detached semantic-row rerun left a closed success artifact with `status=ok`, `code=OK`, `probe_success=true`, `readiness.ready=true`, and a concrete final render | does not prove that the separately deferred `Canva` account-credit hold is solved for standalone thumbnail/background-generation work |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-post-closeout-state-fix-20260620-173718\probe_result.json` + `evidence\result.json` + `evidence\closeout_state.json` | accepted same-run `Sheet1!row15` final closeout for run `8e8a0813-5113-4c1c-ba8b-9e7a16d1ff89`: Stage1 artifacts, downstream service artifacts, render final output, and closeout state are aligned | does not prove all rows, Excel sync-back, standalone Canva/Product Background, or broad browser reliability |
| `D:\YOUTUBEAUTO_RUNTIME\probe\current-run-row15-quoted-20260621-114016\probe_result.json` + `evidence\result.json` + `evidence\closeout_state.json` | fresh `CURRENT_RUN_ACCEPTED` same-run `Sheet1!row15` final closeout for run `b311b5b3-c358-4f31-acc7-6209ed4ddea0`: Stage1 artifacts, downstream service artifacts, render final output, and closeout state are aligned | does not prove all rows, Excel sync-back, standalone Canva/Product Background, or broad browser reliability |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-legacy-video-lane-ulw-20260622-043726\probe_result.json` + `evidence\result.json` + `assets\output\render_final.mp4` | latest fresh `CURRENT_RUN_ACCEPTED` same-run `Sheet1!row15` final closeout for run `8c2f4b5b-4d4d-4695-9ba7-eb021736928f`: `probe_result.json` is `status=ok`, `code=OK`, `exit_code=0`, `probe_success=true`, `ticks=30`; render metadata is `worker_stage=render`, `worker_error_code=OK`, `attempts=0`, `backoff_sec=0.0`, `completion_state=succeeded`, `final_output=true`, `final_artifact=render_final.mp4`, `size=2108944`, `sha256=FA13FD7BD7F1EE5A483C03C3EEC5CC838FCDABFA711D413F2C6662E3E1825EB5` | does not prove all rows, Excel sync-back, standalone Canva/Product Background, or broad browser reliability |
| `D:\YOUTUBEAUTO_RUNTIME\probe\current-proof-row15-post-chatgpt-idle-fix-20260706-012526\probe_result.json` + `evidence\result.json` + `artifacts\chatgpt\chatgpt-sheet1-15\f98b400e-a6e2-4f2f-8371-14ae22005694\raw_output.json` | later fresh development-purpose proof reached ChatGPT capture and failed closed at Stage1 parser with `worker_error_code=missing_scene_prompts`; raw capture was `gpt_capture.status=ok`, `source=gpt_response_text`, `legacy_blocks=[]`, and contained no Stage1 JSON, `[Voice]`, or `[#01]` scene blocks | does not supersede the accepted 2026-06-22 final closeout; it records a new truthful blocker for the current live ChatGPT/topic-only route |
| `D:\YOUTUBEAUTO_RUNTIME\probe\current-proof-row15-topic-only-canary-20260706-060149\probe_result.json` + `evidence\result.json` + `artifacts\chatgpt\chatgpt-sheet1-15\1a575eaf-6695-446b-85f7-d13afa2bf1e8\raw_output.json` | fresh topic-only canary again selected the `롱폼` Custom GPT URL, sent only the row topic, captured `gpt_capture.status=ok`, then failed closed at Stage1 parser with `worker_error_code=missing_scene_prompts`, `attempts=1`, and `backoff_sec=0.0`; raw capture had `legacy_blocks=[]` and no Stage1 scene blocks | confirms the current blocker is the live Custom GPT topic-only output contract, not a downstream generation boundary or a reason to add hidden prompt strengthening |
| `D:\YOUTUBEAUTO_RUNTIME\probe\ulw-selftest-readiness-20260706-132804\probe_result.json` + `D:\YOUTUBEAUTO_RUNTIME\probe\ulw-control-readiness-20260706-132909\probe_result.json` + `C:\Users\1\AppData\Local\Temp\opencode\runtime-readiness-postcontrol-20260706-133024.out.json` | after commit `cdeaa38`, detached selftest refreshed browser health to `healthy_count=5`, detached control once produced idle `run_id=bbc08b71-1e79-4a49-84d7-4d2c08c1d5a7`, `worker_registry.json={}`, `gpt_status.ok_count=3`, and post-control readiness returned `ready=true`, `code=OK`, `blockers=[]` | proves only the operational readiness gate after the bootstrap/control path ran; it does not prove a new row15 E2E closeout, all-row completion, Excel sync-back, Canva availability, or restoration of the live topic-only ChatGPT Stage1 output contract |
| `D:\YOUTUBEAUTO_RUNTIME\probe\task5-row15-post-send-fallback-20260707-185421\probe_result.json` + `evidence\result.json` + `artifacts\chatgpt\chatgpt-sheet1-15\c38acd14-345e-4c16-b33d-27fb9e8cb454\raw_output.json` | after commit `0ceb838`, the fresh Task 5 proof moved past submit (`gpt_capture.status=ok`, `sendClicked=true`, `sendTestId=send-button`, `submit_evidence.classification=sent`, `capture_meta.final_state_code=ok`) and then failed closed at Stage1 parser with `worker_error_code=missing_scene_prompts`; raw capture had `legacy_blocks=[]` and no Stage1 JSON, `[Voice]`, or `[#01]` scene blocks | proves the evidence-less send fallback fix advanced the current run to the next truthful boundary; it still does not restore the live topic-only Custom GPT Stage1 output contract or prove E2E completion |
| `D:\YOUTUBEAUTO_RUNTIME\probe\geminigen-boundary-text-post-polling-fix-direct-20260708-063755\geminigen.out.log` + worker `result.json` + `attach_evidence.json` | after commit `d115dac`, the focused GeminiGen boundary proof ran for about 132 seconds instead of the prior 9.8-second early exit, attached to `https://geminigen.ai/app/video-gen/veo`, filled the prompt, clicked Generate, and failed closed with `worker_error_code=BROWSER_UNHEALTHY`, `final_output=false`, `placeholder_artifact=true`; post-run DOM showed `Generation failed` and `This is an error from Google's side...`, with no `video` element and no mp4 artifact | proves the GeminiGen polling-window code fix removed the early-exit defect and reached a truthful external generation blocker; it does not prove GeminiGen artifact success or E2E completion |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-post-geminigen-polling-proof-quoted-20260708-065155\probe_result.json` + `evidence\result.json` + `artifacts\chatgpt\chatgpt-sheet1-15\7e33fe44-e5c0-4f54-9750-3e4ae4092f76\raw_output.json` | after commit `1e22981`, a fresh detached row15 proof sent the topic-only prompt, captured `gpt_capture.status=ok`, `sendClicked=true`, and `capture_meta.final_state_code=ok`, then failed closed at Stage1 with `worker_error_code=invalid_voice_groups`, `attempts=1`, `backoff_sec=0.0`, `completion.state=blocked`, `next_jobs_count=0`, and `final_output=false`; raw capture was a single unnumbered `[Voice]` prose blob with `legacy_blocks=[]` and no accepted Stage1 JSON or `[#NN]` scene blocks | classifies the current live run as `CURRENT_RUN_BLOCKED` at the external Custom GPT output contract; parser fail-closed behavior is correct and downstream rerun is not allowed while Stage1 is blocked |
| `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-post-browser-recovery-proof-20260708-231538\probe_result.json` + `evidence\result.json` + `state\job_queue.json` + post-failure DOM snapshot via existing GeminiGen CDP `9555` | after the Custom GPT settings save and browser-session recovery, fresh same-run row15 proof `37bd5336-aa85-4217-927d-90a1cf2477de` advanced beyond Stage1 into qwen3_tts, rvc, Genspark, and SeaArt, then closed at `geminigen_adapter / BROWSER_UNHEALTHY`; terminal evidence is `probe_status=failed`, `probe_code=BROWSER_UNHEALTHY`, `probe_success=false`, `probe_exit_code=20`, failed job `geminigen-37bd5336-aa85-4217-927d-90a1cf2477de-32`, `attempts=4`, `backoff_sec=0.0`, `final_output=false`, and no final artifact. The single read-only post-failure DOM snapshot on the existing tab showed URL `https://geminigen.ai/app/video-gen/veo`, title OK, `Generation failed`, `This is an error from Google's side...`, no `video` elements, no download/API-file links, and `Please verify you're human` present. | Oracle reviewed this as `CURRENT_RUN_BLOCKED`; the added DOM evidence supports an external GeminiGen/Google generation failure with Turnstile revalidation boundary, not a Stage1/parser/image/voice/render failure. Full E2E completion is not proven, and runtime_v2 code changes or error-code semantics changes are not justified by this evidence alone. |

## Result

Current closeout retest result for `Sheet1!row15` is reclassified by the accepted fresh detached run:

- `status`: `CURRENT_RUN_ACCEPTED`
- `reading`: accepted only for same-run detached run `8c2f4b5b-4d4d-4695-9ba7-eb021736928f` at `D:\YOUTUBEAUTO_RUNTIME\probe\stage5-row15-legacy-video-lane-ulw-20260622-043726`
- `why`: the accepted run has `Sheet1!row15` seed evidence, Stage1 GPT artifacts, same-run downstream artifacts for `qwen3_tts`, `rvc`, `kenburns`, `genspark`, `seaart`, `geminigen`, terminal `render_final.mp4`, and render metadata aligned to the same `run_id` with `worker_error_code=OK`, `attempts=0`, and `backoff_sec=0.0`.

## Interpretation Rules

- `probe_result.json code=OK` is only accepted as row15 closeout success when it is paired with same-run terminal render or failure artifact and aligned closeout state.
- generic row evidence is not semantic target-row evidence.
- partial downstream artifacts without `probe_result.json` + terminal success/failure artifact do not count as closeout.
- standalone `Canva` hold evidence must not be back-projected into the broader Excel-driven E2E status.
- Browser popup/modal/iframe handling remains code-and-test evidence unless a specific same-run browser evidence artifact proves that boundary.
- Excel sync-back remains unproven for the accepted run because `excel_sync_updated=false` and `stage1_excel_merged=false`.

## Next Allowed Meaning

- This result proves only the accepted `Sheet1!row15` final closeout boundary for run `8c2f4b5b-4d4d-4695-9ba7-eb021736928f`.
- The next allowed meaning is not broader than `CURRENT_RUN_ACCEPTED` for `Sheet1!row15`; any all-row, Excel sync-back, standalone Canva, or broad browser reliability claim requires separate accepted evidence.
- `Canva` remains a separate external credit-hold boundary and must not be reopened without credit/session availability changing.
- Later development-purpose proofs may fail earlier than the accepted closeout when the live external route drifts. In that case, record the first truthful blocker, but do not reinterpret the accepted closeout unless a same-scope replacement proof is produced.
- For the current topic-only ChatGPT route, repeated fresh canaries now stop at `missing_scene_prompts`; runtime_v2 must keep the parser fail-closed and wait for the Custom GPT side to restore legacy Stage1 JSON or legacy `[Voice]`/`[#01]` block output before downstream boundaries are retried.
- The `20260707-185421` Task 5 proof confirms commit `0ceb838` moved the current live path past submit, but the first truthful blocker remains the external topic-only Custom GPT output contract (`missing_scene_prompts`).
- The `20260706-132804` selftest plus `20260706-132909` control-once sequence clears the operational readiness blockers (`WORKER_REGISTRY_MISSING`, `GPT_STATUS_STALE`, `GPT_FLOOR_FAIL`) only after the runtime bootstrap/control path has executed. A read-only readiness check before that path runs may still report stale operational state and should not be treated as a new code failure by itself.
- The `20260708-063755` GeminiGen boundary proof confirms commit `d115dac` removed the post-click polling early-exit defect, but the fresh live boundary stopped at an external GeminiGen/Google generation failure with no video artifact. Do not retry broadly or add runtime fallback logic from this evidence alone.
- The `20260708-065155` fresh row15 detached proof confirms the first current blocker remains Stage1 Custom GPT output shape (`invalid_voice_groups`), not GeminiGen or downstream generation. Keep topic-only input and parser fail-closed; do not run downstream boundaries until Custom GPT emits a valid Stage1 contract.
- The `20260708-231538` post-browser-recovery proof supersedes the prior Stage1 blocker for the current topic-only route by proving Stage1 contract recovery and downstream image/voice progress in the same run, then stopping truthfully at `geminigen_adapter / BROWSER_UNHEALTHY`. A read-only existing-session DOM snapshot confirmed `Generation failed`, `This is an error from Google's side...`, no video/download artifact, and a Turnstile verification prompt. Record this as `CURRENT_RUN_BLOCKED`; do not change runtime_v2 production code or retry broadly from this evidence alone, and do not claim row15 E2E completion without a later same-run terminal render artifact.
